/// <reference types="react-scripts" />

declare module 'disable-scroll'
declare module 'emotion-icons/fa-solid'
declare module 'emotion-icons/ion-md'

// declare global {
//   interface String {
//     trunc(length: number, useDots?: boolean): string
//   }
// }

// eslint-disable-next-line no-extend-native
// String.prototype.trunc = function trunc(length: number, useDots = false): string {
//   return this.length > length ? this.substr(0, length - 1) + (useDots ? '...' : '&hellip;') : this.toString()
// }
